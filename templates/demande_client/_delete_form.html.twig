<form method="post" action="{{ path('app_demande_client_delete', {'id': demande_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_client.id) }}">
    <button class="btn">Delete</button>
</form>
