<form method="post" action="{{ path('app_offre_demande_delete', {'id': offre_demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_demande.id) }}">
    <button class="btn">Delete</button>
</form>
